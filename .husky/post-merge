#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pull main/develop to receive the latest package version after merges.
# When on the default integration branch, prune merged remote branches and
# delete stale local branches (see scripts/git-prune-branches.sh).
sh "$(dirname -- "$0")/../scripts/git-prune-branches.sh" || true
